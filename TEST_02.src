function coolFunc(arr: int[], size: int) => void 
{
	local d : int[10];
	local g : int[10];
	write(2+2);

	while(counter < 3)
	{
		write(2+2);
	};
}

function badFunc() => void 
{

	
}

class a isa LINEAR, TRANFORM { 

	private function a (a : int[10], b : float[], c : float) => poop;

	private function b () => void;

	public constructor (f : float[9]);

	private attribute b : poop[10];

};

class wia { 



};

/* print the array */
function printArray(arr: int[], size: int) => void 
{
  local n: int;
  local i: int; 
  n := size;
  i := 0; 
  while (i<n) { 
    write(2);
      i := i+1;
  };
} 

// main funtion to test above
function main() => void
{
  local arr: int[7]; 
  arr[0] := 64;
  arr[1] := 34;
  arr[2] := 25;
  arr[3] := 12;
  arr[4] := 22;
  arr[5] := 11;
  arr[6] := 90;
 
}


